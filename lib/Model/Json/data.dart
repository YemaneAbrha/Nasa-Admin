class Event {
  final String id;
  final String title;
  final String body;
  final String image;
  Event(this.id, this.title, this.body, this.image);
}

class Message {
  final int id;
  final String body;
  Message(this.id, this.body);
}
